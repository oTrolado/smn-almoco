import { FiltroTrocaPipe } from './filtro-troca.pipe';

describe('FiltroTrocaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTrocaPipe();
    expect(pipe).toBeTruthy();
  });
});
